spring:
  datasource:
    url: jdbc:mysql://localhost:3306/atc_team9?createDatabaseIfNotExist=true
    username: root
    password: asdf1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create # create , none
    #    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace